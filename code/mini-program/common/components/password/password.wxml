<view class="password-6" bindtap='focus'>
  <view wx:for="{{6}}" wx:key="{{index}}">
    <view hidden="{{inputData.length-1<index}}" class="round"></view>
  </view>
</view>

<input type='number' value="{{inputData}}" bindinput='inputHandler' focus='{{isFocus}}' style='display: none;'></input>
